# APRS backend URL
APRS_BACKEND_URL=http://localhost:3001

# Must match GPS_API_KEY in aprs-backend .env (leave empty if backend has no key set)
GPS_API_KEY=

# How long (seconds) to suppress duplicate events for the same DMR-ID
DEBOUNCE_SEC=60

# Log raw DSD+ lines to console (1 = yes)
DEBUG=0
