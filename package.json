{
  "name": "dmr-parser",
  "version": "1.0.0",
  "description": "Reads DSD+ output, decodes DMR-ID, looks up callsign, posts to APRS backend",
  "scripts": {
    "dev":   "tsx src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx":        "^4.15.0",
    "typescript": "^5.4.5"
  }
}
